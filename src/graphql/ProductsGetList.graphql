query ProductsGetList(
	$first: Int!
	$skip: Int!
	$nameContains: String
) {
	products(
		first: $first
		skip: $skip
		where: { name_contains: $nameContains }
	) {
		...ProductListItem
	}
	# zwraca liczbę produktów
	# productsConnection {
	# 	aggregate {
	# 		count
	# 	}
	# }
}
